#include <stdio.h>

int main() {
   
    printf("Comments in C language\n"); // This is a single-line comment.
    
    // The next line will execute normally, as comments do not affect execution.
    printf("C executes this line as well.\n");
    
    /* 
       Sometimes we need
       to use 
       multi-line comments.
    */
    
    return 0;
}
